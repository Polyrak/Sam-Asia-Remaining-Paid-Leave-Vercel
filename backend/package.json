{
  "name": "remaining-paid-leave-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API server that computes remaining paid leave from an OpenProject instance",
  "scripts": {
    "dev": "nodemon --exec \"node scripts/run-with-retry.mjs\"",
    "start": "node src/index.js"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0",
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "nodemonConfig": {
    "ignore": [
      "src/data/*"
    ]
  }
}
